# Network Isolator Web Dashboard + Supervisor Dependencies
# Install with: pip3 install -r requirements.txt
#
# NOTE: PyGObject (gi) and GStreamer Python bindings are NOT pip-installable.
# Install on the Pi with:
#   sudo apt install python3-gi python3-gst-1.0 gir1.2-gst-plugins-base-1.0
# The venv must be created with --system-site-packages so these are visible:
#   python3 -m venv --system-site-packages venv

bokeh>=3.3.0
pandas>=2.0.0
pyyaml>=6.0
pyinotify>=0.9.6
tornado>=6.4
bleak>=0.22.0

# Supervisor extras
aiohttp>=3.9
psutil>=5.9
python-json-logger>=2.0
