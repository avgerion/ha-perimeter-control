# Isolator Supervisor dependencies
# Install with: pip install -r requirements.txt

# FastAPI web framework (replacing Tornado)
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.4.0

# Async web framework (legacy - being replaced by FastAPI)
tornado>=6.4

# Async HTTP client (for http health probes)
aiohttp>=3.9

# YAML config parsing
PyYAML>=6.0

# System metrics (CPU / memory / disk for /api/v1/metrics)
psutil>=5.9

# JSON logging (optional — enables structured log output)
python-json-logger>=2.0
