# Wildlife Monitor Configuration - REFERENCE ONLY
#
# This file shows the SCHEMA for Wildlife Monitor configuration.
# DO NOT edit this file to configure wildlife monitoring.
#
# Instead, edit: config/perimeterControl.conf.yaml
# (This template is for documentation purposes only and is not deployed.)
#
# See docs/CONFIG-TEMPLATES-GUIDE.md for usage examples.

# Wildlife Monitor Configuration
sensors:
  - type: bme280
    address: 0x76
    metrics: [temperature, humidity, pressure]
  - type: motion_pir
    gpio_pin: 18
camera:
  trigger_on_motion: true
  record_duration: 30
  resolution: [1280, 720]
data:
  collection_interval: 300 # 5 minutes
  retention_days: 30
alerts:
  temperature_threshold: 35
  motion_cooldown: 600 # 10 minutes

dashboard:
  server:
    host: "0.0.0.0"
    port: 8094
    type: "bokeh"
  features:
    sensor_charts: true
    motion_timeline: true
    environmental_data: true
    wildlife_gallery: true
  data_refresh_interval: 60
